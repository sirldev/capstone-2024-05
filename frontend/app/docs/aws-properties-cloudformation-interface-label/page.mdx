# `AWS::CloudFormation::Interface Label`

`Label` 는 파라미터 그룹 또는 파라미터의 이름을 정의하는 ParameterGroup 및 ParameterLabel 속성의 속성입니다.

## 통사론

### JSON (영문)

```
{
  "default" : String
}
```

## 속성

`default`  
CloudFormation 콘솔에서 파라미터 그룹 또는 파라미터의 이름을 지정하는 데 사용하는 기본 레이블입니다.  
*필수 항목 *여부: 아니요  
_유형_: 문자열
