# AWS::EC2::ClientVpnTargetNetworkAssociation

Client VPN 엔드포인트와 연결할 대상 네트워크를 지정합니다. 대상 네트워크는 VPC의 서브넷입니다. 동일한 VPC의 여러 서브넷을 Client VPN 엔드포인트와 연결할 수 있습니다. 각 가용 영역에는 하나의 서브넷만 연결할 수 있습니다. 가용 영역 중복성을 제공하기 위해 두 개 이상의 서브넷을 연결하는 것이 좋습니다.

## 통사론

AWS CloudFormation 템플릿에서 이 엔터티를 선언하려면 다음 구문을 사용합니다.

### JSON (영문)

```
{
  "Type" : "AWS::EC2::ClientVpnTargetNetworkAssociation",
  "Properties" : {
      "ClientVpnEndpointId" : String,
      "SubnetId" : String
    }
}
```

## 속성

`ClientVpnEndpointId`  
Client VPN 엔드포인트의 ID입니다.  
_필수 항목 여부_: 예  
_유형_: 문자열  
_업데이트 필요 사항_: 바꿔 놓음

`SubnetId`  
Client VPN 엔드포인트와 연결할 서브넷의 ID입니다.  
_필수 항목 여부_: 예  
_유형_: 문자열  
_업데이트 필요 사항_: 바꿔 놓음

## 반환 값

### 참고문헌

이 리소스의 논리적 ID를 내장 함수에 전달하면 `Ref` `Ref`연결 ID가 반환됩니다. 예를 들면 다음과 같습니다 `cvpn-assoc-1234567890abcdef0`.

함수 사용에 대한 자세한 `Ref`내용은 Ref를 참조하세요.

## 예제

### 대상 서브넷을 Client VPN 엔드포인트와 연결

다음 예제에서는 대상 네트워크를 Client VPN 엔드포인트와 연결합니다.

#### JSON (영문)

```
"myNetworkAssociation": {
    "Type": "AWS::EC2::ClientVpnTargetNetworkAssociation",
    "Properties": {
        "ClientVpnEndpointId": {
            "Ref": "myClientVpnEndpoint"
        },
        "SubnetId": {
            "Ref": "mySubnet"
        }
    }
}
```

## 참고 항목

- AWS Client VPN 관리자 안내서*의 Client VPN 시작하기 *
- AWS Client VPN 관리자 안내서*의 대상 네트워크 *
