# AWS::EC2::인스턴스 NoDevice

AMI의 블록 장치 매핑에 포함된 지정된 장치를 억제합니다. 장치를 억제하려면 빈 문자열을 지정합니다.

`NoDevice` 은 Amazon EC2 BlockDeviceMapping 속성의 속성입니다.

## 통사론

AWS CloudFormation 템플릿에서 이 엔터티를 선언하려면 다음 구문을 사용합니다.

### JSON (영문)

```
{
}
```
